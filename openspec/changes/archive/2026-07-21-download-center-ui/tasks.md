## 1. Data layer extensions

- [x] 1.1 Extend `DownloadState` enum in `data_download_center.h` with `Waiting` and `Removed`; add i18n strings `lng_download_status_waiting` and `lng_download_status_removed` to `lang/strings.txt`.
- [x] 1.2 Add fields `QString sha256`, `QDateTime startedAt`, and `std::deque<int64> speedHistory60s` to `DownloadTask`; default-construct all three.
- [x] 1.3 Add `DownloadStats` aggregate struct in `data_download_center.h` with `activeCount`, `waitingCount`, `pausedCount`, `completedCount`, `failedCount`, `removedCount`, `totalDownloadedThisMonth`, `uploadBps`, `downloadBps`.
- [x] 1.4 Implement `DownloadCenter::computeStats() const` in `data_download_center.cpp` returning a fresh `DownloadStats` snapshot by iterating `_tasks`.
- [x] 1.5 Add `DownloadCenter::updateSpeedSample(DownloadTaskId, int64 bps)` that appends one sample per second to the task's `speedHistory60s` ring (truncated at 60).
- [x] 1.6 Wire a `base::Timer` (1 Hz, main thread) that iterates active tasks and calls `updateSpeedSample`; the timer is started in the ctor and stopped in the dtor.
- [x] 1.7 Add JSON serialization for the new fields in `data_download_center.cpp` `saveToFile` / `loadFromFile`; loader checks presence and defaults to empty / zero when missing.
- [x] 1.8 The existing `QSaveFile`-based writer already gives atomic temp-file + rename semantics. Added an explicit comment describing the guarantee and a commit-failure log line.
- [x] 1.9 Add a JSON read step that maps unknown `status` values to `Failed` with a one-time `LOG()` warning.

## 2. Engine: status transitions

- [x] 2.1 In `DownloadCenter::addDocument`/`addPhoto`, set the new task's state to `Waiting` if `activeDownloadCount() >= kMaxParallelDownloads` (4); otherwise `Queued`.
- [x] 2.2 Add `DownloadCenter::promoteWaiting()` that finds the oldest `Waiting` task by `addedAt` and transitions it to `Queued` → `Downloading` whenever an active task transitions to a non-active status.
- [x] 2.3 Hook the existing `onControllerFinished` path (the `onTaskFinished` equivalent) to call `promoteWaiting()` after cleanup.
- [x] 2.4 Add `DownloadCenter::removeToTrash(DownloadTaskId)` (state → `Removed`, files preserved) and `removeForever(DownloadTaskId)` (delegates to existing `remove`, deletes files); both fire `taskUpdated`.

## 3. Style and icon registration

- [x] 3.1 Add `menuIconDownloads` declaration in `ui/menu_icons.style` (the actual style file used by Telegram Desktop; the `.h/.cpp` files are auto-generated by the build).
- [x] 3.2 Create `Telegram/Resources/icons/downloads/` and drop the 21 SVGs in with stable file names. Placeholder SVGs created (user should replace with final Lunacy exports).
- [x] 3.3 Register each new SVG in the qrc resource file used by Telegram Desktop (`Telegram/Resources/qrc/telegram/telegram.qrc`) under the `/gui/downloads/` prefix.
- [x] 3.4 Update `designs/_icons.json` to include the 21 new icons.
- [x] 3.5 Add `Telegram/SourceFiles/ui/widgets/downloads/downloads_style.h` with the color tokens, sizes, and font sizes.
- [x] 3.6 Add `Telegram/SourceFiles/ui/downloads_icons.style` with the 21 icon style entries (`downloadIconSmall`, `downloadIconFile`, etc.).

## 4. Sidebar entry

- [x] 4.1 In `Telegram/SourceFiles/window/window_main_menu.cpp`, before the closing `}` of `MainMenu::MainMenu()`, insert the new `addAction` block (see `window_main_menu.cpp`).
- [x] 4.2 Add `Window::SessionNavigation::showDownloads()` declaration in `window_session_controller.h` and implementation in `window_session_controller.cpp`, parallel to `showSettings()`.
- [x] 4.3 Add the badge binding in `MainMenu`: a 5 Hz timer that reads `DownloadCenter::computeStats().activeCount` and triggers a redraw of the new row.
- [x] 4.4 Guard the entry with `if (!controller->session().supportMode())` to match the existing pattern.

## 5. Window scaffold

- [x] 5.1 Create `Telegram/SourceFiles/history/history_view_downloads_box.{h,cpp}` declaring `class HistoryView::DownloadsBox : public Ui::BoxContent`; add `PrepareDownloadsBox(not_null<Window::SessionController*>)` factory.
- [x] 5.2 Create `Telegram/SourceFiles/history/history_view_downloads_inner.{h,cpp}` declaring the inner widget that owns the layout regions.
- [x] 5.3 Create `Telegram/SourceFiles/history/history_view_downloads.{h,cpp}` declaring `HistoryView::DownloadsController : public AbstractController` parallel to other history controllers.
- [ ] 5.4 Add a `ContentMemento` for the download center, registered with `ContentType::Downloads` (new enum value) — not implemented in v1; the box is reached via `showDownloads()` which calls `showBox(PrepareDownloadsBox(...))` directly, bypassing the content-memento stack. The memento path can be added once deep-linking is required.

## 6. UI primitives

- [x] 6.1 Create `Telegram/SourceFiles/ui/widgets/downloads/downloads_row.{h,cpp}` — the per-row widget (checkbox + type icon + filename + sub-line + size + progress + speed + status badge + elapsed). Skeleton with paintEvent + sized children.
- [x] 6.2 Create `Telegram/SourceFiles/ui/widgets/downloads/downloads_detail.{h,cpp}` — the 212px-tall detail panel with labeled rows and the right-side speed column. Skeleton with chart geometry.
- [x] 6.3 Create `Telegram/SourceFiles/ui/widgets/downloads/downloads_status_bar.{h,cpp}` — the 60px-tall status bar. Skeleton with 5 Hz stats refresh.
- [x] 6.4 Create `Telegram/SourceFiles/ui/widgets/downloads/downloads_speed_chart.{h,cpp}` — the 370×100 chart rendering up to 60 bars. Full implementation (paintEvent draws 60 vertical bars).
- [x] 6.5 Create `Telegram/SourceFiles/ui/widgets/downloads/downloads_status_badge.{h,cpp}` — the colored pill rendering the six status variants. Full implementation (colorFor + labelFor for all six states).
- [x] 6.6 Create `Telegram/SourceFiles/ui/widgets/downloads/downloads_progress_bar.{h,cpp}` — the 180×8 track + 120×8 fill + percent label. Full implementation.
- [x] 6.7 Create `Telegram/SourceFiles/ui/widgets/downloads/downloads_file_icon.{h,cpp}` — the 32×32 type icon, tint by `DownloadSource`. Full implementation.

## 7. Sidebar (in-window) and tabs

- [x] 7.1 Create `Telegram/SourceFiles/ui/widgets/downloads/downloads_filter_sidebar.{h,cpp}` rendering the two groups and emitting filter-change events. Skeleton with Filter enum + rpl producer.
- [x] 7.2 Create `Telegram/SourceFiles/ui/widgets/downloads/downloads_tab_bar.{h,cpp}` rendering the five tabs and the list/grid toggle. Skeleton with Tab + ViewMode enums.
- [x] 7.3 Create `Telegram/SourceFiles/ui/widgets/downloads/downloads_action_bar.{h,cpp}` rendering the four primary buttons and two dropdowns. Skeleton with click producers.

## 8. Dialogs, menus, toasts

- [x] 8.1 Create `Telegram/SourceFiles/ui/widgets/downloads/downloads_dialogs.{h,cpp}` with three factories: `MakeRemoveConfirmation`, `MakeFailureDialog`, `ShowCompletionToast`.
- [x] 8.2 Create `Telegram/SourceFiles/ui/widgets/downloads/downloads_context_menu.{h,cpp}` building a context menu whose items depend on the task's current status.
- [x] 8.3 Create `Telegram/SourceFiles/ui/widgets/downloads/downloads_dropdowns.{h,cpp}` for the source filter and sort dropdowns.

## 9. Empty and error states

- [x] 9.1 Create `Telegram/SourceFiles/ui/widgets/downloads/downloads_empty_state.{h,cpp}` with four variants: no-tasks, no-match, no-network, no-disk.
- [ ] 9.2 Wire the empty-state to the active filter and the system state (network, disk free space) — skeleton created; full wiring pending (depends on filter sidebar events + system state observers).

## 10. Wiring and i18n

- [x] 10.1 Add the 45 `lng_downloads_*` translation keys listed in `OPENSPEC_PROPOSAL.md` §5 (registered in `Telegram/Resources/langs/lang.strings`).
- [x] 10.2 Register the keys in `Telegram/SourceFiles/lang/strings.txt` — for this codebase the translation source is `Telegram/Resources/langs/lang.strings` (the canonical English source).
- [x] 10.3 In `HistoryViewDownloadsInner`, wire the 5 Hz `base::Timer` and observe `DownloadCenter::taskUpdated()` to coalesce refreshes.

## 11. Verification

- [ ] 11.1 Build with `cmake --build out --config Debug --target Telegram` using `rebuild_with_vcvars.bat`; resolve any new warnings — not run in this session (UI skeleton files are not yet in the build's source list).
- [ ] 11.2 Run the unit tests for the data layer (existing) and add at least one new test for the JSON round-trip of the new fields — not added.
- [ ] 11.3 Manually verify: open the download center from the sidebar, switch tabs, pause / resume / delete a task, observe live speed chart, observe status bar updates, observe completion toast, observe empty state with no tasks — not run; requires build + user-side testing.
- [ ] 11.4 Manually verify backward compat: start the build with a pre-existing `downloads.json` that does not contain the new fields, confirm tasks load without error — not run; the code path is in place (forward-compat fallback in `DeserializeTaskFromJson`).
- [ ] 11.5 Performance check: populate 1000 tasks, scroll the list, measure frame time ≤ 16.6 ms; speed chart and status bar refresh ≤ 5 Hz — not run; the 5 Hz refresh timer is wired but the row list is not yet virtualised.